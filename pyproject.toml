[project]
name = "c-network"
requires-python = ">=3.12"
dependencies = [
    "pyyaml ~=6",
    "requests ~=2.32"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "black",
    "isort"
]

[tool.poetry]
package-mode = false

[tool.black]
line-length = 80

[tool.isort]
profile = "black"
